{
  "name": "@tenet/models-bedrock",
  "version": "0.0.0",
  "description": "AWS Bedrock ChatModel adapter (Anthropic Claude family). Accepts an injected invoker so callers wire their own AWS SDK or gateway.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "echo 'no lint yet' && exit 0"
  }
}
