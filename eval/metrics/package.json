{
  "name": "@tenet/eval-metrics",
  "version": "0.0.0",
  "description": "Pure scorers — one per BENCHMARKS dimension. Numbers in, MetricResult out. No I/O.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "echo 'no lint yet' && exit 0"
  }
}
