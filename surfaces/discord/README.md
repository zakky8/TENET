# @tenet/surface-discord

Discord Surface adapter. No discord.js hard dep — inject any client implementing `DiscordClient`. Renders citations as Discord Markdown `• [label](url)` since Discord does not accept HTML.
