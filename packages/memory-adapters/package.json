{
  "name": "@tenet/memory-adapters",
  "version": "0.0.0",
  "description": "External memory adapters — mem0, Letta, Zep. Generic ExternalMemoryAdapter implements SemanticMemory; per-vendor providers ship the HTTP calls.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "echo 'no lint yet' && exit 0"
  },
  "dependencies": {
    "@tenet/memory": "workspace:*"
  }
}
