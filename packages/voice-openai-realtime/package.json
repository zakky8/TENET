{
  "name": "@tenet/voice-openai-realtime",
  "version": "0.0.0",
  "description": "OpenAI Realtime API adapter — replaces separate ASR + TTS + LLM with one WebSocket. Sub-second latency voice agent.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } },
  "files": ["dist", "README.md"],
  "dependencies": { "@tenet/voice": "workspace:*" },
  "keywords": [
    "agent-framework", "ai", "ai-agents", "langchain-alternative", "llm",
    "openai", "openai-realtime", "tenet", "typescript", "voice", "voice-agent",
    "websocket"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "echo 'no lint yet' && exit 0"
  }
}
