{
  "name": "@tenet/tools-mcp-gateway",
  "version": "0.0.0",
  "description": "MCP tool gateway — per-tenant allow-list + OAuth iss validation (RFC 9207 / 2026-07-28 spec RC) + audit logging.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "echo 'no lint yet' && exit 0"
  },
  "dependencies": { "@tenet/tools-mcp": "workspace:*" }
}
