{
  "name": "@tenet/streaming",
  "version": "0.0.0",
  "description": "Streaming ChatModel contract + SSE parser + structured-output incremental validation — closes the streaming gap vs Pydantic-AI / Mastra / OpenAI Agents SDK.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "echo 'no lint yet' && exit 0"
  }
}
