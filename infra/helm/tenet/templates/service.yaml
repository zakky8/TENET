apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenet.fullname" . }}
  labels:
    {{- include "tenet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tenet.selectorLabels" . | nindent 4 }}
